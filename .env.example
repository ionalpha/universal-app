# --- API ---
PORT=8787

# Frontend -> API base URL (used by apps/web and apps/shell)
VITE_API_URL=http://localhost:8787

# --- Data: libSQL / Turso ---
# Local dev uses a file; cloud uses a Turso libsql:// URL + auth token.
DATABASE_URL=file:./local.db
TURSO_AUTH_TOKEN=
